import { CreateUserForm } from "@/components";

export const CreateUser = () => {
  return (
    <div className="mt-24 flex h-full flex-grow flex-col items-center justify-center">
      <CreateUserForm />
    </div>
  );
};
